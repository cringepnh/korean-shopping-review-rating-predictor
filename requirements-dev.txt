# Minimal dependencies for running the pure-Python test suite (tests/) in CI.
# Deliberately excludes torch/transformers/datasets/accelerate: those are only
# needed to train or run the model, not to test data_utils.py, decoding.py,
# reporting.py, or coral.py's array-only helper functions. This keeps CI fast
# and runnable without a GPU wheel.
pytest>=8.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
