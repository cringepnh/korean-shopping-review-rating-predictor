# Pinned to the exact versions this project was built and evaluated with, for
# reproducibility. torch is pinned to its base version only (not the +cu124
# local build tag) so this installs on CPU-only or different-CUDA machines
# too; for a matching CUDA 12.4 GPU build, install torch separately first
# from https://pytorch.org/get-started/locally/, then run this file.
torch==2.6.0
transformers==5.3.0
datasets==4.6.1
accelerate==1.13.0
pandas==3.0.1
numpy==2.3.5
scikit-learn==1.8.0
scipy==1.17.1
requests==2.34.2
huggingface_hub==1.28.0
